request:
  language: cpp
  source: "#include <iostream>\nint main() { int a = 0; std::cout << \"done\"; return 0; }"
  source_filename: solution.cpp
  artifact_filename: solution
  build:
    limits:
      wall_time_s: 2
      memory_kb: 10240
      max_processes: 1
    flags:
      - "--std=c++11"
  run:
    limits:
      wall_time_s: 5
      memory_kb: 524288
      max_processes: 64
  tests:
    - stdin: ""
      expected_stdout: "done"

expected:
  status: success
  tests:
    - status: accepted
      stdout: "done"
