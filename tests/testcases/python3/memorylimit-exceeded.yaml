request:
  language: py3
  source: |
    x = bytearray(512 * 1024 * 1024)
  source_filename: solution.py
  run:
    limits:
      wall_time_s: 9
      memory_kb: 262144
      max_processes: 64
  tests:
    - stdin: ""
      expected_stdout: ""

expected:
  status: success
  tests:
    - status: memory_exceeded
